{
  "name": "@gewis/aurora",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "yarn workspace @gewis/aurora-core dev",
    "start": "yarn workspace @gewis/aurora-core start",
    "tsoa": "yarn workspace @gewis/aurora-core tsoa",
    "seed": "yarn workspace @gewis/aurora-core seed",
    "seed:gewis": "yarn workspace @gewis/aurora-core seed:gewis",
    "seed:hubble": "yarn workspace @gewis/aurora-core seed:hubble",
    "validate": "yarn workspace @gewis/aurora-core validate",
    "build": "yarn workspaces foreach -A run build",
    "typecheck": "yarn workspaces foreach -A run typecheck",
    "lint": "yarn workspaces foreach -A run lint",
    "lint-fix": "yarn workspaces foreach -A run lint-fix",
    "format": "yarn workspaces foreach -A run format",
    "format-fix": "yarn workspaces foreach -A run format-fix",
    "test": "yarn workspaces foreach -A run test",
    "test:watch": "yarn workspaces foreach -A run test:watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@vitest/coverage-v8": "^4.0.18",
    "concurrently": "^9.2.1",
    "eslint": "^9.39.2",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "nodemon": "^3.1.11",
    "prettier": "^3.8.1",
    "ts-node": "^10.9.2",
    "tsx": "^4.21.0",
    "typescript": "5.9.3",
    "vitest": "^4.0.18"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "packageManager": "yarn@4.12.0"
}
